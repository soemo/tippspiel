- if user_signed_in?
  %p
    -#TODO soeren 20.04.12 Short Ranking
    -#TODO soeren 20.04.12 today Games (getTodayGames)
-else
  = form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class=>""}) do |f|
    = devise_error_messages!
    = f.label :email
    = f.text_field :email, :class=>"span3"
    = f.label :password
    = f.password_field :password, :class=>"span3"

    - if devise_mapping.rememberable?
      %label.checkbox
        = f.check_box :remember_me
        = f.label :remember_me

    %p= f.submit "Sign in", :class => "btn"

    = render :partial => "devise/shared/links"