- content_for :hero_image do
  %section.hero

    -# Space placeholder
    .row.show-for-small-only
      .medium-12.columns.space-small
    .row.show-for-medium
      .medium-12.columns.space-medium

    .row
      .medium-offset-3.medium-9.large-offset-5.large-7.columns
        .callout
          %h2
            = "#{I18n.t(:signed_in_hello)} #{@presenter.user_name}"
          .row
            .small-12.columns
              = link_to(icon('list-ol', I18n.t('full_ranking_list'), {class: 'fa-fw'}), rankings_path)
            .small-12.columns
              = link_to(icon('line-chart',I18n.t(:your_ranking_per_game), {class: 'fa-fw'}), ranking_per_game_path(current_user))

          - if @presenter.tournament_finished?
            %br
            %p
              %b
                AUUUSSS AUUUSSS - das Spiel ist aus!
            %p
              Der Pokal wird schnellstmöglich den Weg zum Gewinner finden. Allen Anderen danke ich für die Teilnahme und
              hoffe es hat Spaß gemacht. Bis zum nächsten Mal in 2 Jahren.

    -# Space placeholder
    .row.show-for-small-only
      .medium-12.columns.space-small
    .row.show-for-medium
      .medium-12.columns.space-medium

.row
  .small-12.columns
    %h3= t('your_tips')

    = render partial: 'champion_tip', locals: {presenter: @presenter}
    = render partial: 'user_tips', locals: {presenter: @presenter}
