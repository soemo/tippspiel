%section.hero{'data-interchange' => "[#{asset_path('hero-image-green-gras.jpg')}, (small)], [#{asset_path('hero-image-green-gras.jpg')}, (medium)], [#{asset_path('hero-image-green-gras.jpg')}, (large)]"}

  -# Space placeholder
  .row.show-for-small-only
    .medium-12.columns.space-small
  .row.show-for-medium
    .medium-12.columns.space-medium

  .row
    .small-centered.medium-uncentered.medium-6.large-7.columns
      = render partial: 'welcome_text_infos'
      - if @presenter.tournament_finished?
        %p
          %b
            AUUUSSS AUUUSSS - das Spiel ist aus! Der Sieger steht fest!

        - if current_user.present?
          %p
            %b
              Trommelwirbel - Herzlichen Glückwunsch!
          %p
            Der Pokal wird schnellstmöglich den Weg zum Gewinner finden. Allen Anderen danke ich für die Teilnahme,
            hoffe es hat Spaß gemacht und bis zum nächsten Mal in 2 Jahren.

    .small-centered.medium-uncentered.medium-6.large-5.columns
      = render partial: 'user_infos', locals: {presenter: @presenter}

  -# Space placeholder
  .row.show-for-small-only
    .medium-12.columns.space-small
  .row.show-for-medium
    .medium-12.columns.space-medium